components:
  schemas:
    Element:
      required:
        - element_id
        - content
        - apply_state
        - feedback_id
      properties:
        element_id:
          type: string
          description: 요소 고유 아이디
        content:
          type: string
          description: 내용
        apply_state:
          type: integer
          description: 요소 적용 상황
        image_id:
          type: string
          description: 이미지 고유 아이디
        feedback_id:
          type: string
          description: 피드백 고유 아이디
